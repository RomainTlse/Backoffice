import { InteretDirective } from './interet.directive';

describe('InteretDirective', () => {
  it('should create an instance', () => {
    const directive = new InteretDirective();
    expect(directive).toBeTruthy();
  });
});
