import { FormationDirective } from './formation.directive';

describe('FormationDirective', () => {
  it('should create an instance', () => {
    const directive = new FormationDirective();
    expect(directive).toBeTruthy();
  });
});
