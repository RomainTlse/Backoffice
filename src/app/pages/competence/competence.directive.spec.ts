import { CompetenceDirective } from './competence.directive';

describe('CompetenceDirective', () => {
  it('should create an instance', () => {
    const directive = new CompetenceDirective();
    expect(directive).toBeTruthy();
  });
});
